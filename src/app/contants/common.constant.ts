export enum DATE_FORMAT {
  FULL_DATE = 'dd/MM/yyyy',
  DATE_TIME = 'dd/MM/yyyy HH:mm',
  FULL_DATE_TIME = 'dd/MM/yyyy HH:mm:ss',
  CUSTOM_DATE_TIME = 'HH:mm - dd/MM',
}

export enum EMode {
  VIEW = 'view',
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum EClosePopupCode {
  CANCEL = 'cancel',
  SAVE = 'save',
  CONFIRM = 'confirm',
}
